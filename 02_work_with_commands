type    — это встроенная команда, которая сообщает тип указанной ей команды.
    type ls
    type type
    type cd

which   — определение местоположения выполняемого файла
    which type
    which ls

help    — получение справки для встроенных команд
    help cd
    help ls
    help python

--help  — вывод инструкции по использованию
    cd --help
    ls --help

man — вывод страниц справочного руководства
    man ls
    man help
    man python

man раздел искомый_термин
    Организация справочного руководства:
    Раздел  Содержит
    1       Пользовательские команды
    2       Программные интерфейсы системных вызовов в ядре
    3       Программные интерфейсы в библиотеке C
    4       Специальные файлы, такие как узлы устройств и драйверы
    5       Форматы файлов
    6       Игры и развлечения, такие как хранители экрана
    7       Прочее
    8       Команды системного администрирования

    man 5 passwd // выведет страницу с описанием формата файла /etc/passwd.

apropos — вывод списка подходящих команд
    apropos partition
    apropos usb

whatis — вывод очень краткого описания команды
    whatis ls

README и другие файлы с описанием программ
    Многие программные пакеты, установленные в вашей системе, включают файлы с документацией, размещаемые в каталоге /usr/share/doc. 

 alias - создание собственных команд
    alias foo='cd /usr; ls; cd -' // создать алиас
    unalias foo // удалить алиас